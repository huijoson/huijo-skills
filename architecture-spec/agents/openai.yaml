interface:
  display_name: "Architecture Spec"
  short_description: "Create plain-language, architect-ready HTML specs"
  default_prompt: "Use $architecture-spec to create a two-layer architecture specification for this change."
policy:
  allow_implicit_invocation: true
